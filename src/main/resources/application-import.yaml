spring:
  main:
    web-application-type: none
  docker:
    compose:
      lifecycle-management: start-only

jwt:
  secret: import-only-dummy-secret-key-not-used-but-must-be-long-enough-256bit

spot-import:
  enabled: true
