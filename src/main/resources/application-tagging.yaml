spring:
  main:
    web-application-type: none
  docker:
    compose:
      enabled: false

jwt:
  secret: tagging-only-dummy-secret-key-not-used-but-must-be-long-enough-256bit

spot-tagging:
  enabled: true
