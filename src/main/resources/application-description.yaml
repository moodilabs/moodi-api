spring:
  main:
    web-application-type: none
  docker:
    compose:
      enabled: false

jwt:
  secret: description-only-dummy-secret-key-not-used-but-must-be-long-enough-256bit

spot-description:
  enabled: true
  locale: en-US
  concurrency: 3
